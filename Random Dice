#VSC

import random

roll  = random.randit(1,6)
guess = int(input("Guess the dice roll:\n"))

if guess == roll:
  print("Correct! They rolled a " + str(roll))

else:
  print("Worng! They rolled a " + str(roll))
